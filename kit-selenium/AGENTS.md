# Selenium 模块指引

- 修改浏览器启动或关闭逻辑时，验证成功、超时、中断及构建失败后的浏览器、driver、调试端口与进程释放。
- 测试命令、跳过条件与手工脚本限制见[浏览器验证](../docs/development.md#browser-testing)；手工脚本不能作为验证入口。
- 真实浏览器验证使用独立临时 profile，仅清理本次创建并拥有的进程；不沿用示例中的本机路径或清理用户已有 profile。
- 测试保持独立 JPMS 模块和 module path，通过公开 API 验证；不为测试扩大生产模块的开放范围。
